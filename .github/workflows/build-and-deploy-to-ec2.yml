name: Build and Deploy to Cloudflare


on:
  push:
    branches:
      - master

env:
  INSTANCE_ID: ${{ secrets.EC2_INSTANCE_ID }}
  SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
  EC2_USER: ${{ secrets.EC2_USER }}
  EC2_HOST: ${{ secrets.EC2_HOST }}

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Locking requirements
        run: uv pip compile pyproject.toml -o requirements.txt

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ secrets.AWS_ECR_REPO }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

#      - name: Set up SSH key
#        run: |
#          mkdir -p ~/.ssh
#          echo "$SSH_KEY" > ~/.ssh/id_rsa
#          chmod 600 ~/.ssh/id_rsa
#
#      - name: Add EC2 to known hosts
#        run: |
#          ssh-keyscan $EC2_HOST >> ~/.ssh/known_hosts
#
#      - name: Copy source code to EC2
#        run: |
#          rsync -avz --exclude '.git*' ./ $EC2_USER@$EC2_HOST:/home/$EC2_USER/app
#
#      - name: Build and run Docker container on EC2
#        run: |
#          ssh $EC2_USER@$EC2_HOST << 'EOF'
#            cd ~/app
#            docker stop fastapi-app || true
#            docker rm fastapi-app || true
#            docker build -t fastapi-app .
#            docker run -d --name fastapi-app -p 8000:8000 \
#              -e TURN_KEY_ID=${{ secrets.TURN_KEY_ID }} \
#              -e TURN_KEY_API_TOKEN=${{ secrets.TURN_KEY_API_TOKEN }} \
#              -e CLOUDLFARED_TUNNEL_ID=${{ secrets.CLOUDFLARED_TUNNEL_ID }} \
#              -e CLOUDFLARE_TOKEN=${{ secrets.CLOUDFLARE_TOKEN }} \
#              --restart always fastapi-app
#          EOF
